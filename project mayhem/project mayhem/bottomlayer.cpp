#include <SFML/Graphics.hpp>

#include "options.h"
#include "bottomlayer.h"
#include "resources.h"

/*
Bottomlayer::Bottomlayer(float x, float y, const sf::Texture& texture, const Options& options) : Object(x, y, texture, tileSize), options(options)
{
	priority = 0;
}

void Bottomlayer::Draw(sf::RenderWindow& window)
{
	// Call the object version of Draw first
	Object::Draw(window);

}

*/